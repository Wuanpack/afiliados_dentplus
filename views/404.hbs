
<div class="text-center py-5">
  <h1 class="display-4 text-danger">404</h1>
  <p class="lead">{{message}}</p>
  {{#if session.userId}}
    <a href="/affiliates" class="btn btn-primary">Ir a afiliados</a>
  {{else}}
    <a href="/" class="btn btn-primary">Ir al inicio</a>
  {{/if}}
</div>
